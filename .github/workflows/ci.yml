name: CI

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  merge_group:
    branches: [main]

jobs:
  lint:
    if: github.event_name == 'merge_group' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "CI passed!"
